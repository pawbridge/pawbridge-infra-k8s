pid /tmp/nginx.pid;
error_log /dev/stderr warn;
events { worker_connections 128; }
http {
 access_log off;
 proxy_connect_timeout 10s;
 proxy_read_timeout 15s;
 proxy_send_timeout 15s;
 proxy_ssl_server_name on;
 proxy_ssl_verify on;
 proxy_ssl_trusted_certificate /etc/ssl/certs/ca-certificates.crt;
 proxy_pass_request_headers off;
 server {
  listen 8080;
  server_tokens off;
  client_max_body_size 16k;
  add_header Cache-Control no-store always;
  location = /health { return 200 'UP'; }
  location = /token {
   limit_except POST { deny all; }
   proxy_set_header Host oauth2.googleapis.com;
   proxy_set_header Content-Type application/x-www-form-urlencoded;
   proxy_set_header Authorization $http_authorization;
   proxy_pass https://oauth2.googleapis.com/token;
  }
  location = /userinfo {
   limit_except GET { deny all; }
   proxy_set_header Host openidconnect.googleapis.com;
   proxy_set_header Authorization $http_authorization;
   proxy_pass https://openidconnect.googleapis.com/v1/userinfo;
  }
  location = /jwks {
   limit_except GET { deny all; }
   proxy_set_header Host www.googleapis.com;
   proxy_pass https://www.googleapis.com/oauth2/v3/certs;
  }
  location / { return 404; }
 }
}
