apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: animal-service-test
  namespace: argocd
spec:
  project: pawbridge-dev
  source:
    repoURL: https://github.com/pawbridge/pawbridge-infra-k8s.git
    targetRevision: dev
    path: charts/animal-service
    helm:
      releaseName: animal-service
      valueFiles:
      - ../../environments/dev/isolated-values/animal-service.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: pawbridge-dev
  syncPolicy:
    syncOptions:
    - Prune=false
    - FailOnSharedResource=true
