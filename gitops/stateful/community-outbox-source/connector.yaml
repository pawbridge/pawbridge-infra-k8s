apiVersion: kafka.strimzi.io/v1
kind: KafkaConnector
metadata:
  name: community-outbox-connector
  namespace: kafka
  labels:
    strimzi.io/cluster: pawbridge-connect
    app.kubernetes.io/name: community-outbox-connector
    app.kubernetes.io/part-of: pawbridge
spec:
  class: io.debezium.connector.mysql.MySqlConnector
  tasksMax: 1
  # Manually sync only after the corrected PostEventConsumer image is deployed.
  # The consumer must read the expanded EventRouter JSON body directly.
  state: running
  config:
    database.hostname: mysql.databases.svc.cluster.local
    database.port: 3306
    database.user: '${secrets:kafka/store-mysql-cdc-auth:username}'
    database.password: '${secrets:kafka/store-mysql-cdc-auth:password}'
    database.connectionTimeZone: Asia/Seoul
    database.server.id: 2003
    topic.prefix: pawbridge-community
    snapshot.mode: no_data
    database.include.list: pawbridge_community
    table.include.list: pawbridge_community.outbox_events
    schema.history.internal.kafka.bootstrap.servers: pawbridge-kafka-bootstrap.kafka.svc.cluster.local:9092
    schema.history.internal.kafka.topic: schema-changes.community.outbox
    transforms: outbox
    transforms.outbox.type: io.debezium.transforms.outbox.EventRouter
    transforms.outbox.table.field.event.id: event_id
    transforms.outbox.table.field.event.key: aggregate_id
    transforms.outbox.table.field.event.timestamp: created_at
    transforms.outbox.table.field.event.payload: payload
    transforms.outbox.table.fields.additional.placement: type:header:eventType
    transforms.outbox.route.topic.replacement: community.post.events
    transforms.outbox.route.by.field: aggregate_type
    transforms.outbox.table.expand.json.payload: true
    key.converter: org.apache.kafka.connect.json.JsonConverter
    key.converter.schemas.enable: false
    value.converter: org.apache.kafka.connect.json.JsonConverter
    value.converter.schemas.enable: false
