apiVersion: kafka.strimzi.io/v1
kind: KafkaConnector
metadata:
  name: community-outbox-postgresql-connector
  namespace: kafka
  labels:
    strimzi.io/cluster: pawbridge-connect
    app.kubernetes.io/part-of: pawbridge
spec:
  class: io.debezium.connector.postgresql.PostgresConnector
  tasksMax: 1
  state: stopped
  autoRestart:
    enabled: false
  config:
    database.hostname: pawbridge-postgresql.databases.svc.cluster.local
    database.port: '5432'
    database.user: ${secrets:kafka/community-postgresql-cdc-auth:username}
    database.password: ${secrets:kafka/community-postgresql-cdc-auth:password}
    database.dbname: pawbridge
    plugin.name: pgoutput
    topic.prefix: pawbridge-pg-community
    schema.include.list: pawbridge_community
    table.include.list: pawbridge_community\.outbox_events
    slot.name: pawbridge_community_outbox
    slot.drop.on.stop: 'false'
    publication.name: pawbridge_community_outbox
    publication.autocreate.mode: disabled
    snapshot.mode: no_data
    tombstones.on.delete: 'false'
    errors.tolerance: none
    max.batch.size: '256'
    max.queue.size: '1024'
    max.queue.size.in.bytes: '16777216'
    heartbeat.interval.ms: '10000'
    transforms: outbox
    predicates: isOutbox
    predicates.isOutbox.type: org.apache.kafka.connect.transforms.predicates.TopicNameMatches
    predicates.isOutbox.pattern: pawbridge-pg-community\.pawbridge_community\.outbox_events
    transforms.outbox.predicate: isOutbox
    transforms.outbox.type: io.debezium.transforms.outbox.EventRouter
    transforms.outbox.table.field.event.id: event_id
    transforms.outbox.table.field.event.key: aggregate_id
    transforms.outbox.table.field.event.payload: payload
    transforms.outbox.table.fields.additional.placement: type:header:eventType
    transforms.outbox.route.topic.replacement: community.post.events
    transforms.outbox.route.by.field: aggregate_type
    transforms.outbox.table.expand.json.payload: 'true'
    transforms.outbox.table.op.invalid.behavior: fatal
    key.converter: org.apache.kafka.connect.json.JsonConverter
    value.converter: org.apache.kafka.connect.json.JsonConverter
    value.converter.schemas.enable: 'false'
    header.converter: org.apache.kafka.connect.storage.SimpleHeaderConverter
    key.converter.schemas.enable: 'false'
