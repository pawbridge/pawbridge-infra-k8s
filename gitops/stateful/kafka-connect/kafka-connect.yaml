apiVersion: kafka.strimzi.io/v1
kind: KafkaConnect
metadata:
  name: pawbridge-connect
  namespace: kafka
  labels:
    app.kubernetes.io/name: pawbridge-kafka-connect
    app.kubernetes.io/part-of: pawbridge
  annotations:
    argocd.argoproj.io/sync-wave: "0"
    strimzi.io/use-connector-resources: "true"
spec:
  version: 4.1.2
  image: quay.io/strimzi/kafka@sha256:39bce6f044fe43c62b1355b4e855866899685f316db15cd25a27f7f37fffa99f
  replicas: 1
  bootstrapServers: pawbridge-kafka-bootstrap:9092
  groupId: pawbridge-connect
  configStorageTopic: connect-configs
  offsetStorageTopic: connect-offsets
  statusStorageTopic: connect-status
  config:
    config.storage.replication.factor: 1
    offset.storage.replication.factor: 1
    status.storage.replication.factor: 1
    key.converter: org.apache.kafka.connect.json.JsonConverter
    value.converter: org.apache.kafka.connect.json.JsonConverter
    key.converter.schemas.enable: false
    value.converter.schemas.enable: false
  resources:
    requests:
      cpu: 200m
      memory: 512Mi
    limits:
      cpu: "1"
      memory: 1Gi
  logging:
    type: inline
    loggers:
      rootLogger.level: INFO
  template:
    pod:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
    connectContainer:
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop:
            - ALL
        runAsNonRoot: true
