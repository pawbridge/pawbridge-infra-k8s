pawbridge_snapshot_migration:
  cluster:
    - monitor
    - cluster:admin/repository/*
    - cluster:admin/snapshot/*
  indices:
    - names: ["*"]
      privileges: [read, view_index_metadata, monitor]
      allow_restricted_indices: true
